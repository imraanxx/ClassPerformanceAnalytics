{
  "response": "# Full Architecture: Class Performance Analytics Dashboard\n\nThis document outlines the complete, production-ready architecture for the Class Performance Analytics Dashboard. It follows a **Three-Tier Client-Server Architecture** separating the frontend (React), backend (Node.js/Express), and database (PostgreSQL).\n\n---\n\n## 1. File and Folder Structure\n\nThe project is structured as a monorepo containing two main directories: `/frontend` and `/backend`.\n\n### Frontend (React.js + Tailwind CSS)\n```text\n/frontend\n\u2514\u2500\u2500 src/\n    \u2514\u2500\u2500 assets/             # Static files (images, school logos)\n    \u2514\u2500\u2500 components/         # Reusable UI elements (Buttons, Inputs, Modals, Cards)\n    \u2514\u2500\u2500 features/           # Domain-specific modules\n        \u2514\u2500\u2500 auth/           # Login form, password reset\n        \u2514\u2500\u2500 dashboard/      # Chart.js components, AI Alert cards, Leaderboards\n        \u2514\u2500\u2500 quick-log/      # Attendance toggles, 3-star participation UI\n        \u2514\u2500\u2500 gradebook/      # CA score spreadsheet-style input\n        \u2514\u2500\u2500 reports/        # PDF generation components and layouts\n    \u2514\u2500\u2500 context/            # React Context API (Global State)\n        \u2514\u2500\u2500 AuthContext.js  # Manages user session & RBAC (Admin vs Teacher)\n        \u2514\u2500\u2500 DataContext.js  # Manages globally fetched class/student data\n    \u2514\u2500\u2500 hooks/              # Custom React hooks (e.g., useFetch, useAuth)\n    \u2514\u2500\u2500 pages/              # Main view routes\n        \u2514\u2500\u2500 Login.jsx\n        \u2514\u2500\u2500 TeacherDashboard.jsx\n        \u2514\u2500\u2500 AdminDashboard.jsx\n    \u2514\u2500\u2500 services/           # Axios HTTP client configuration\n        \u2514\u2500\u2500 apiClient.js    # Base Axios instance with JWT interceptors\n        \u2514\u2500\u2500 studentApi.js   # Endpoints for getting/updating student records\n        \u2514\u2500\u2500 aiApi.js        # Endpoints to fetch AI advisor insights\n    \u2514\u2500\u2500 utils/              # Helper functions\n        \u2514\u2500\u2500 pdfExport.js    # Logic for html2pdf.js / jsPDF generation\n        \u2514\u2500\u2500 formatData.js   # Date and percentage formatters\n    \u2514\u2500\u2500 App.jsx             # Main React Router setup\n```\n\n### Backend (Node.js + Express.js + PostgreSQL)\n```text\n/backend\n\u2514\u2500\u2500 src/\n    \u2514\u2500\u2500 config/             # Environment variables and DB connection (e.g., Prisma client)\n    \u2514\u2500\u2500 controllers/        # Handles incoming requests & sends responses\n        \u2514\u2500\u2500 authController.js\n        \u2514\u2500\u2500 dataEntryController.js # Handles saving Quick-Log & Gradebook data\n        \u2514\u2500\u2500 aiController.js   # Manages requests to the external AI API\n        \u2514\u2500\u2500 adminController.js # Handles bulk CSV student uploads\n    \u2514\u2500\u2500 middlewares/        # Express middlewares\n        \u2514\u2500\u2500 authVerify.js     # Validates JWT tokens\n        \u2514\u2500\u2500 roleCheck.js      # Ensures only Admins can access certain routes\n    \u2514\u2500\u2500 models/             # Database Schemas (using Prisma ORM)\n        \u2514\u2500\u2500 schema.prisma     # Defines Users, Students, Classes, Attendance, Scores\n    \u2514\u2500\u2500 routes/             # API URL definitions\n        \u2514\u2500\u2500 authRoutes.js     # /api/auth/login\n        \u2514\u2500\u2500 entryRoutes.js    # /api/entry/attendance, /api/entry/grades\n        \u2514\u2500\u2500 aiRoutes.js       # /api/ai/insights\n        \u2514\u2500\u2500 adminRoutes.js    # /api/admin/upload-csv\n    \u2514\u2500\u2500 services/           # Heavy business logic\n        \u2514\u2500\u2500 aiService.js      # Formats data, sanitizes PII, calls OpenAI API\n        \u2514\u2500\u2500 csvParser.js      # Parses Admin uploaded CSVs into JSON for DB injection\n    \u2514\u2500\u2500 server.js           # Express app initialization and port listening\n```\n\n---\n\n## 2. What Each Part Does\n\n*   **Frontend `/features`:** This is where the core functionality lives. `quick-log` contains the specific UI for the 1-click attendance toggles and 3-star ratings. `dashboard` contains the Chart.js logic to plot attendance against CA scores.\n*   **Frontend `/utils/pdfExport.js`:** This script takes the HTML/CSS of the current dashboard view, renders it onto an invisible HTML canvas, and converts it into a downloadable PDF for the teacher.\n*   **Backend `/controllers/adminController.js` & `/services/csvParser.js`:** These handle the pre-population of student data. The Admin uploads an Excel/CSV file; the parser turns it into database rows so the Teacher does not have to type student names.\n*   **Backend `/services/aiService.js`:** This is the brain of the Engagement Advisor. It fetches recent attendance and CA scores from PostgreSQL, strips out names (for privacy), converts the data into a text prompt, sends it to the AI (e.g., OpenAI API), and returns the generated \"Intervention Alert\" to the frontend.\n\n---\n\n## 3. Where State Lives\n\nState is managed depending on its scope to optimize performance and prevent unnecessary re-renders:\n\n1.  **Global UI & Session State (React Context API):**\n    *   `AuthContext`: Stores the current logged-in user's JWT token, User ID, and Role (`Teacher` or `Admin`). This dictates which sidebars and routes are accessible.\n    *   `ThemeContext`: (Optional) Manages light/dark mode.\n2.  **Server State (React Query or Axios + DataContext):**\n    *   Fetched data (Class Rosters, historical attendance, CA scores) is fetched via Axios and temporarily cached. When a teacher updates a score, the state is invalidated and refetched to ensure the dashboard charts update instantly.\n3.  **Local Component State (React `useState`):**\n    *   Used inside the `Quick-Log` and `Gradebook` components to hold pending inputs (e.g., checking/unchecking attendance boxes) before the teacher clicks the \"Save\" button to batch-send the data to the backend.\n\n---\n\n## 4. How Services Connect (The Data Flow)\n\nHere is the step-by-step connection map of how data moves through the system:\n\n**Scenario:** *Teacher inputs a 1-star participation score, which triggers an AI Alert.*\n\n1.  **Client-Side (React UI):** The Teacher clicks the \"1-Star\" button for a student in the `Quick-Log` component and hits \"Save.\"\n2.  **API Client (Axios):** `studentApi.js` packages this data into a JSON payload and attaches the Teacher's JWT token to the HTTP headers. It sends a `POST` request to `/api/entry/grades`.\n3.  **Middleware (Express):** The `authVerify.js` middleware intercepts the request, checks the JWT token, confirms the Teacher is authorized, and passes it to the Controller.\n4.  **Backend Controller & DB (Node + Prisma):** `dataEntryController.js` receives the data and uses Prisma ORM to save the 1-star rating into the PostgreSQL database.\n5.  **AI Service Trigger (Node):** The controller automatically pings `aiService.js`. The service queries the DB for that student's past 4 weeks of data, noticing a drop. It sends an anonymized prompt to the **OpenAI API**.\n6.  **AI Response:** The AI API returns a prescriptive alert string (e.g., *\"Warning: Participation dropped, schedule intervention.\"*)\n7.  **Client Update:** The Express server sends a `200 OK` response back to the React frontend, containing the \"Saved successfully\" message along with the new AI Alert. \n8.  **Re-Render:** React updates the `DataContext`, immediately re-rendering the Chart.js graphs and displaying a red notification badge in the AI Alert Center.\n"
}