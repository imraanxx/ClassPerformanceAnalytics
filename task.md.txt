{
  "response": "# Granular MVP Implementation Plan: Class Performance Analytics Dashboard\n\nThis plan is designed for an Engineering LLM. Each task is a vertically sliced, atomic unit of work with a clear testable outcome. Pass these tasks to the LLM **one by one**.\n\n---\n\n## Phase 1: Project Skeleton & Database Setup\n\n### Task 1: Initialize Monorepo and Backend Boilerplate\n*   **Concern:** Basic backend folder and dependencies.\n*   **Action:** Create a root folder. Inside, create a `/backend` folder. Initialize `npm`. Install `express`, `cors`, `dotenv`, and `nodemon`. Create a basic `server.js` that listens on port 5000.\n*   **Test:** Run `npm start`. Navigate to `http://localhost:5000` and see a \"Server is running\" text response.\n\n### Task 2: Initialize Frontend Boilerplate\n*   **Concern:** Basic React and Tailwind setup.\n*   **Action:** In the root, run Vite to create a `/frontend` React app. Install `tailwindcss`, `postcss`, and `autoprefixer`. Configure `tailwind.config.js`. Replace `App.jsx` with a simple \"Hello Tailwind\" component utilizing Tailwind classes.\n*   **Test:** Run `npm run dev`. Navigate to `http://localhost:5173` and see the styled \"Hello Tailwind\" text.\n\n### Task 3: Database & Prisma Setup\n*   **Concern:** Connect the backend to PostgreSQL.\n*   **Action:** In `/backend`, install `prisma` and `@prisma/client`. Initialize Prisma. Add a local PostgreSQL connection string to `.env`. \n*   **Test:** Run `npx prisma db pull` (if DB exists) or `npx prisma format` to ensure the CLI connects and parses `.env` without throwing errors.\n\n### Task 4: Define Prisma Schema\n*   **Concern:** Create the database models.\n*   **Action:** In `schema.prisma`, define models: `User` (id, name, role, email, password), `Class` (id, name, teacherId), `Student` (id, name, matric_no, classId), `Attendance` (id, date, status, studentId), `Participation` (id, week, stars, studentId), and `Assessment` (id, title, score, studentId).\n*   **Test:** Run `npx prisma db push`. Verify using your SQL client (e.g., pgAdmin, DBeaver) that the tables are created successfully.\n\n### Task 5: Database Seeding Script\n*   **Concern:** Mock data for development.\n*   **Action:** Create a `seed.js` script in `/backend`. Seed 1 Admin user, 1 Teacher user, 1 Class (\"CSC 201\"), and 5 mock Students linked to that class. \n*   **Test:** Run `node seed.js`. Check the database to confirm the rows exist.\n\n---\n\n## Phase 2: Authentication & Routing (Minimal MVP)\n\n### Task 6: Backend Login API\n*   **Concern:** Authenticate users.\n*   **Action:** Install `jsonwebtoken`. Create a route `POST /api/auth/login`. It should accept `{ email, password }`, check against the `User` DB table (plaintext password match is fine for *this specific MVP step* to save time, or use `bcrypt` if preferred), and return a JWT containing the user's ID and Role.\n*   **Test:** Use Postman/cURL to send a POST request with the seeded teacher's email. Verify a JWT token is returned.\n\n### Task 7: Frontend Auth Context & Routing\n*   **Concern:** Client-side state and protected routes.\n*   **Action:** Install `react-router-dom` and `axios`. Create an `AuthContext.js` to store the JWT and User Role in `localStorage`. Create a `Login.jsx` page. Create protected routes for `TeacherDashboard` and `AdminDashboard`.\n*   **Test:** Type fake credentials \u2192 see error. Type seeded Teacher credentials \u2192 get redirected to `/teacher-dashboard`.\n\n---\n\n## Phase 3: Admin Features (Data Pre-population)\n\n### Task 8: Backend CSV Upload API\n*   **Concern:** Parse student rosters.\n*   **Action:** Install `multer` and `csv-parser`. Create `POST /api/admin/upload-csv`. The endpoint should accept a CSV file, parse the rows (name, matric_no), and bulk insert them into the `Student` table linked to a specific `classId`.\n*   **Test:** Use Postman to upload a dummy CSV file. Query the database to ensure the new students are inserted.\n\n### Task 9: Frontend Admin CSV Upload UI\n*   **Concern:** UI for Admin to upload rosters.\n*   **Action:** Build `AdminDashboard.jsx` with a file input and a \"Upload Class Roster\" button. Connect it via Axios to the backend endpoint.\n*   **Test:** Login as Admin, select a CSV file, click Upload. View success toast/alert.\n\n---\n\n## Phase 4: Teacher Quick-Log & Gradebook\n\n### Task 10: Backend Fetch Roster API\n*   **Concern:** Serve students for a specific teacher's class.\n*   **Action:** Create `GET /api/classes/:classId/students`. It should return a JSON array of students belonging to that class.\n*   **Test:** Ping the endpoint in Postman; receive an array of seeded students.\n\n### Task 11: Frontend Quick-Log UI\n*   **Concern:** The 1-click attendance and 3-star participation interface.\n*   **Action:** Create a `QuickLog.jsx` component. Fetch the roster on mount. Render a list. Each row has a checkbox (Attendance: default checked) and three star icons (Participation). Maintain local React state for changes.\n*   **Test:** Load the page, see the list of students, toggle checkboxes, and click stars. Ensure local state updates correctly via React DevTools.\n\n### Task 12: Backend Save Quick-Log API\n*   **Concern:** Persist attendance and participation.\n*   **Action:** Create `POST /api/entry/quick-log`. Accept an array of objects `{ studentId, attendanceStatus, participationStars, date }`. Insert/update rows in the `Attendance` and `Participation` tables using Prisma transactions.\n*   **Test:** Send a mocked JSON array via Postman. Check DB to ensure records were written.\n\n### Task 13: Frontend Quick-Log Integration\n*   **Concern:** Connect UI to API.\n*   **Action:** Add a \"Save Quick-Log\" button to `QuickLog.jsx`. On click, format the local state and POST to the backend endpoint.\n*   **Test:** Change attendance/stars on the UI, click Save, refresh the page (or check DB) to ensure data persists.\n\n### Task 14: Backend & Frontend Gradebook\n*   **Concern:** Input CA scores.\n*   **Action:** \n    *   *Backend:* Create `POST /api/entry/grades` to accept `{ studentId, title, score }`.\n    *   *Frontend:* Create `Gradebook.jsx`. Fetch roster, render rows with a number input field. Add a \"Save Grades\" button to POST data.\n*   **Test:** Enter a score of \"15\" for a student, hit save. Verify in DB.\n\n---\n\n## Phase 5: Analytics Dashboard (Chart.js)\n\n### Task 15: Backend Analytics Aggregation API\n*   **Concern:** Prepare data for Chart.js.\n*   **Action:** Create `GET /api/analytics/:classId`. Write a Prisma query that calculates the average attendance percentage, average participation stars, and latest test scores for the class. Return structured JSON.\n*   **Test:** Ping endpoint in Postman. Verify JSON contains structured averages/arrays.\n\n### Task 16: Frontend Dual-Axis Chart Integration\n*   **Concern:** Render the data visually.\n*   **Action:** Install `chart.js` and `react-chartjs-2`. In `TeacherDashboard.jsx`, fetch the analytics data. Render a Line Chart (Attendance/Participation trend) overlaid with a Bar Chart (CA Scores) using dual Y-axes.\n*   **Test:** Open Teacher Dashboard. Verify the chart renders and accurately reflects the dummy data seeded earlier.\n\n---\n\n## Phase 6: AI Engagement Advisor\n\n### Task 17: Backend AI Service Integration\n*   **Concern:** Connect to external AI (e.g., OpenAI).\n*   **Action:** Install `openai` (or `axios` for another API). Create an `aiService.js`. Write a function that takes a student's recent data, formats it into a string (e.g., \"Student missed 2 classes and got 40% on CA\"), and sends it to the AI API with a prompt asking for a 2-sentence intervention alert. Create endpoint `GET /api/ai/insights/:classId`.\n*   **Test:** Ping endpoint. Ensure a text string like \"Schedule a 1-on-1 meeting...\" is returned. *(Tip: Instruct the LLM to use a mock response if no API key is provided yet).* \n\n### Task 18: Frontend AI Alert Panel\n*   **Concern:** Display the AI's advice.\n*   **Action:** Create an `AIAlerts.jsx` component in the Teacher Dashboard. Fetch data from the AI endpoint and render it inside styled notification cards (red/yellow borders based on severity).\n*   **Test:** Load dashboard, verify the AI text appears in a notification card.\n\n---\n\n## Phase 7: Batch PDF Reporting\n\n### Task 19: Frontend PDF Generation\n*   **Concern:** Export dashboard to PDF.\n*   **Action:** Install `html2pdf.js` (or `jspdf` + `html2canvas`). Add a \"Download Class Report\" button to the Teacher Dashboard. Write an `exportPDF` function that targets the dashboard DOM node and triggers the download.\n*   **Test:** Click the \"Download\" button. Open the downloaded PDF and verify the charts and data are legible."
}